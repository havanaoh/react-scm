import axios, { AxiosResponse } from "axios";

export const searchApi = async <T>(api: string, param: object) => {
    // try {
    //     const result: AxiosResponse<T> = await axios.post(api, param);
    // } catch (error) {}
};
